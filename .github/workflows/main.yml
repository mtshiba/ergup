name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Execute the script (Ubuntu/MacOS)
        if: runner.os != 'Windows'
        run: |
          python bin/ergup310.pyc && rm -rf ~/.erg && python ergup.py

          export PATH=$PATH:~/.erg/bin
          export ERG_PATH=~/.erg

          echo 'print! "hello, world!"' | erg
      - name: Execute the script (Windows)
        if: runner.os == 'Windows'
        run: |
          python bin/ergup310.pyc && Remove-Item -Force -Recurse %USERPROFILE%\.erg && python ergup.py

          $env:PATH+=";%USERPROFILE%\.erg\bin"
          $env:ERG_PATH="%USERPROFILE%\.erg"

          echo 'print! "hello, world!"' | erg
